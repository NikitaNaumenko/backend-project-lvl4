version: '3'
services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: project-lvl-4-dev
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

