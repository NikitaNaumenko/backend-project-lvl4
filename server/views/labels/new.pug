extends ../layouts/application.pug

block header
  = t('views.labels.new.header')

block content
  +formFor()(action=route('labels') method='post')
    +input(user, 'name', t('entities.labels.name'), errors)(type="text")
    +submit(user)(value=t('views.labels.new.submit'))

